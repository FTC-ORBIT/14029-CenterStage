package org.firstinspires.ftc.teamcode.autonomous.camera;

import org.firstinspires.ftc.teamcode.autonomous.camera.ElementPosition;
import org.opencv.core.Point;

public class ObjCenter {
    /*
    private ElementPosition elementPosition = ElementPosition.LEFT;
    private static Point center;
    public static void findObjCenter() {
        center = Contours.getCenter(
                Contours.contourPolyList(
                        Contours.getBiggestContour(
                                Contours.getContour(Pipeline.getClonedMat(), Constants.lowHSV, Constants.highHSV)
                        )
                )
        );
        if (center.x > 640) {
            elementPosition = ElementPosition.MIDDLE;
        } else if (center.x < 640) {
            elementPosition = ElementPosition.RIGHT;
        } else {
            elementPosition = ElementPosition.LEFT;
        }
    }

     */
}
